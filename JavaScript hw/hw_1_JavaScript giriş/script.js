let bookTitle 
let pageCount 
let e_book 

bookTitle = String ("Qürur və Qərəz")
pageCount = Number(166)
e_book = Boolean(1)
console.log(`Mənim oxuduğum kitabın adı ${bookTitle}-dir, onun ${pageCount} səhifəsi var. Bu elektron kitabdır: ${e_book}`)


bookTitle = String("Leyla ilə Məcnun")
pageCount = Number(500)
e_book = Boolean(0)
console.log(`Mənim oxuduğum kitabın adı ${bookTitle}-dur, onun ${pageCount} səhifəsi var. Bu elektron kitabdır: ${e_book}`)
